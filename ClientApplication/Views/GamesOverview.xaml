<UserControl x:Class="ClientApplication.Views.GamesOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ClientApplication.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:GamesOverviewViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="680" d:DesignWidth="1280"
             Background="White">
    <Grid ShowGridLines="True" x:Name="GameGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition>
            </ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
    </Grid>
</UserControl>
