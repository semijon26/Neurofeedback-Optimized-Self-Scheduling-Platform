namespace ServerApplication.TestConfigs;

//Should be deleted when the config file structure is ready and only the config file is used
public static class TestConfigs
{
    public static int minConnectedClients = 1;
}